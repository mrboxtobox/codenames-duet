{
  "name": "codenames-duet",
  "version": "1.0.0",
  "description": "Codenames: Duet implementation using Cloudflare technology",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241218.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.10"
  }
}